<%= form_with(model: herd) do |form| %>
  <% if herd.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(herd.errors.count, "error") %> prohibited this herd from being saved:</h2>

      <ul>
        <% herd.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <label>
    Name
    <%= form.text_field :name %>
  </label>

  <div>
    <%= form.submit %>
  </div>
<% end %>
